#variables

num1 = 12
num2= 14

add_res = num1 + num2

print(add_res) # we can also say : print(num1 + num_2) directly but we used this because we don't know may be the value of add_res will find useful

#constants

const d = 13
print(d) # this will show syntax error because in the Python, there is no constant. By converting the variable into the uppercase it will be treated as constant.

D = 13
print(D)